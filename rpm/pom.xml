<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	       xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <packaging>rpm</packaging>
  <modelVersion>4.0.0</modelVersion>
  <artifactId>pth_05</artifactId>
  <version>${revision}${sha1}${changelist}</version>
  <name>pth_05</name>
  <description>pth_05</description>
  <groupId>com.teragrep</groupId>
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <revision>0.0.1</revision>
    <changelist>-SNAPSHOT</changelist>
    <sha1/>
  </properties>
  <build>
    <directory>${project.basedir}/target</directory>
    <plugins>
      <plugin>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>3.4.1</version>
        <executions>
          <execution>
            <id>enforce</id>
            <phase>none</phase>
          </execution>
          <execution>
            <id>enforce-maven</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireMavenVersion>
                  <version>3.2.5</version>
                </requireMavenVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>rpm-maven-plugin</artifactId>
        <version>2.2.0</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <id>default-rpm</id>
            <goals>
              <goal>rpm</goal>
            </goals>
            <phase>package</phase>
          </execution>
        </executions>
        <configuration>
          <summary>${project.groupId}-${project.artifactId}</summary>
          <name>${project.groupId}-${project.artifactId}</name>
          <version>${project.version}</version>
          <release>${env.BUILD_ID}</release>
          <license>Proprietary</license>
          <distribution>Teragrep pth_05</distribution>
          <vendor>https://teragrep.com/</vendor>
          <packager>Teragrep &lt;servicedesk@teragrep.com&gt;</packager>
          <group>Teragrep/pth_05</group>
          <description>${project.groupId}-${project.artifactId}</description>
          <needarch>true</needarch>
          <defaultUsername>srv-s3gw</defaultUsername>
          <defaultGroupname>srv-s3gw</defaultGroupname>
          <defaultFilemode>0644</defaultFilemode>
          <defaultDirmode>0755</defaultDirmode>
          <mappings>
            <mapping>
              <directory>/usr/lib/systemd/system</directory>
              <directoryIncluded>false</directoryIncluded>
              <sources>
                <source>
                  <location>${project.basedir}/src/main/resources/pth_05.service</location>
                </source>
              </sources>
            </mapping>
            <mapping>
              <directory>/opt/teragrep/${project.artifactId}/etc</directory>
              <configuration>noreplace</configuration>
              <directoryIncluded>true</directoryIncluded>
              <filemode>600</filemode>
              <dirmode>700</dirmode>
              <username>srv-s3gw</username>
              <groupname>srv-s3gw</groupname>
              <recurseDirectories>true</recurseDirectories>
              <sources>
                <source>
                  <location>${project.basedir}/src/main/resources/pth_05.properties</location>
                </source>
                <source>
                  <location>${project.basedir}/src/main/resources/authorize.example.json</location>
                </source>
                <source>
                  <location>${project.basedir}/src/main/resources/credentials.example.json</location>
                </source>
                <source>
                  <location>${project.basedir}/src/main/resources/logback.xml</location>
                </source>
              </sources>
            </mapping>
            <mapping>
              <directory>/opt/teragrep/${project.artifactId}/etc/lookup.example</directory>
              <configuration>noreplace</configuration>
              <directoryIncluded>true</directoryIncluded>
              <filemode>600</filemode>
              <dirmode>700</dirmode>
              <username>srv-s3gw</username>
              <groupname>srv-s3gw</groupname>
              <recurseDirectories>true</recurseDirectories>
              <sources>
                <source>
                  <location>${project.basedir}/src/main/resources/lookup.example/example_hosts.json</location>
                </source>
                <source>
                  <location>${project.basedir}/src/main/resources/lookup.example/example_indexes.json</location>
                </source>
              </sources>
            </mapping>
            <mapping>
              <directory>/opt/teragrep/${project.artifactId}/share</directory>
              <directoryIncluded>true</directoryIncluded>
              <filemode>755</filemode>
              <dirmode>755</dirmode>
              <username>srv-s3gw</username>
              <groupname>srv-s3gw</groupname>
              <recurseDirectories>true</recurseDirectories>
              <sources>
                <source>
                  <location>${project.basedir}/../target/pth_05-jar-with-dependencies.jar</location>
                </source>
              </sources>
            </mapping>
            <mapping>
              <directory>/opt/teragrep/${project.artifactId}/bin</directory>
              <directoryIncluded>true</directoryIncluded>
              <filemode>755</filemode>
              <dirmode>755</dirmode>
              <username>srv-s3gw</username>
              <groupname>srv-s3gw</groupname>
              <recurseDirectories>true</recurseDirectories>
              <sources>
                <source>
                  <location>${project.basedir}/../src/main/python/generate_index_mapping.py</location>
                </source>
              </sources>
            </mapping>
          </mappings>
          <requires>
            <require>java-11-openjdk</require>
          </requires>
          <preinstallScriptlet>
            <script>
getent group srv-s3gw > /dev/null || groupadd -r srv-s3gw;
getent passwd srv-s3gw > /dev/null || useradd -r -g srv-s3gw -d /opt/teragrep/${project.artifactId} -s /sbin/nologin -c "Service User" srv-s3gw;
if [ ! -d /opt/teragrep/${project.artifactId}/logs ]; then
    mkdir -p /opt/teragrep/${project.artifactId}/logs;
    chown srv-s3gw:srv-s3gw /opt/teragrep/${project.artifactId}/logs;
    chmod 700 /opt/teragrep/${project.artifactId}/logs;
fi;
exit 0;
            </script>
          </preinstallScriptlet>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
